import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  screenTitle: {
    textAlign: 'center',
    fontSize: 24,
    marginTop: 16,
    color: 'white'
  }
});

export default styles;